#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../skeleton/bin/skeleton" "$@"
  ret=$?
else 
  node  "$basedir/../skeleton/bin/skeleton" "$@"
  ret=$?
fi
exit $ret
